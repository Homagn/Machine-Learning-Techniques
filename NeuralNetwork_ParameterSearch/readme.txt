1. You should have latest version of Keras running on theano backend in python
2. The class neural networks is created as an object and run with the parameters being passed as a list
3. Change elements in the list passparam to obtain different results and then run it.
4. Convergence rates are plotted in performance folder and model description and weights matrix are saved in the model and weights folder. 